<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.xtechcn.microservice.fast</groupId>
        <artifactId>dependencies</artifactId>
        <relativePath>dependencies/pom.xml</relativePath>
        <version>${revision}</version>
    </parent>


    <artifactId>xtechcn-cloud</artifactId>
    <modelVersion>4.0.0</modelVersion>
    <version>${revision}</version>
    <packaging>pom</packaging>


    <modules>
        <module>dependencies</module>
        <module>single-business</module>
    </modules>

    <properties>
        <!-- 编译参数配置 -->
        <openjdk.image>openjdk:21-jdk</openjdk.image>

        <!-- 如果需要使用私有仓库或harbor等之类的 -->
        <docker.host>http://192.168.1.3:2375</docker.host>

        <!-- 阿里云镜像仓库 -->
        <!--
        <docker.registry>registry.cn-hangzhou.aliyuncs.com</docker.registry>
        <docker.namespace>xtechcn-single</docker.namespace>
        <docker.username>your_username</docker.username>
        <docker.password>your_password</docker.password>
        <docker.jdk-image>${openjdk.image}</docker.jdk-image>
        -->

        <!-- 华为云镜像仓库 -->
        <docker.registry>swr.cn-southwest-2.myhuaweicloud.com</docker.registry>
        <docker.namespace>xtechcn-single</docker.namespace>
        <docker.username>your_username</docker.username>
        <docker.password>your_password</docker.password>
        <!-- 构建镜像的基础镜像 swr.cn-southwest-2.myhuaweicloud.com/xtechcn-public/openjdk:21-jdk -->
        <docker.jdk-image>${docker.registry}/xtechcn-public/${openjdk.image}</docker.jdk-image>

        <!--  默认忽略docker构建 -->
        <docker.skip>false</docker.skip>
    </properties>

    <!-- 发布代码 -->
    <distributionManagement>
        <!-- 发布环境仓库地址 -->
        <repository>
            <id>rdc-releases</id>
            <name>rdc-release</name>
            <url>https://packages.aliyun.com/maven/repository/2058464-release-Btjhsk/</url>
        </repository>
        <!-- 快照版本仓库地址 -->
        <snapshotRepository>
            <id>rdc-snapshots</id>
            <name>rdc-snapshots</name>
            <url>https://packages.aliyun.com/maven/repository/2058464-snapshot-YTQtrM/</url>
        </snapshotRepository>
    </distributionManagement>

    <build>
        <finalName>${project.name}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <pluginManagement>
            <plugins>
                <!-- 发布 jar （class 文件）到中央仓库 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <!-- 避免反射中获取 method.getParameters() 无法获取到参数名称 -->
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <!-- 发布 源码（.java）到中央仓库 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <phase>compile</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- 版本号动态管理 https://maven.apache.org/maven-ci-friendly.html -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>${flatten-maven-plugin.version}</version>
                    <configuration>
                        <updatePomFile>true</updatePomFile>
                        <flattenMode>resolveCiFriendliesOnly</flattenMode>
                    </configuration>
                    <executions>
                        <execution>
                            <id>flatten</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>flatten</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>flatten.clean</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>